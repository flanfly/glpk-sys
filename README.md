glpk-sys
========

[GLPK 5.0](https://www.gnu.org/software/glpk/) bindings for Rust. Requires GLPK
headers and library to be installed (`apt install libglpk-dev`, `dnf install
glpk-devel`, `brew install glpk`, or set `GLPK_DIR`).
